# DSA Practise

## Compilation steps
### without cmake
```sh
    gcc <filename.c> -o <filename>
```
### with cmake
A cmake build script has been provided

```sh
    mkdir build
    cd build
    cmake ..
    ninja # or make if ninja doesn't work
```
